#include "include/stealth.h"

bool Stealth::HollowProcess(const std::wstring& targetPath, const std::vector<char>& payload, LPVOID& remoteBase, HANDLE& hProc) {
    // Process hollowing işlemi burada yapılacak (detaylı kod eklenecek)
    return false;
}

bool Stealth::GhostProcess(const std::wstring& targetPath, const std::vector<char>& payload, LPVOID& remoteBase, HANDLE& hProc) {
    // Process ghosting işlemi burada yapılacak (detaylı kod eklenecek)
    return false;
}
